
-- Insert data into departments
INSERT INTO departments (department_name)
SELECT 'Department ' || g
FROM generate_series(1, 100) g;



-- Insert data into employees
INSERT INTO employees (employee_name, department_id)
SELECT 'Employee ' || g, (g % 100) + 1
FROM generate_series(1, 1000000) g;

















Amazon Aurora 


db's:
1) gpss
2) postgres
3) rds admin



service credentials



roles and permissions

table creation and data types , constraints:
1) generated by default as identity(....., cache 1)
2) collate
3) character varying(255)


1) tablespace 
2) schema vs table space
3) asc or desc nulls last



timezones:
change zones from utc to ist etc

IST question asked -> explanation needed



triggers:
use cases like auditing
2) use same trigger on multiple schemas -> asked by lavanya
3) trigger function to transfer data from one table to another -> asked by lady senior
4) 











1) normalization


1) trigger then trigger function